import javax.swing.*;

public interface Icons {
    ImageIcon YOSHI = new ImageIcon(Icons.class.getResource("/yoshi-pushing-chomp.gif"));
    ImageIcon PENGUIN = new ImageIcon(Icons.class.getResource("/penguin-spinning.gif"));
}
